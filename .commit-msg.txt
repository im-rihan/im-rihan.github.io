fix: eliminate all remaining set-state-in-effect warnings

Navbar + CommandPaletteLauncher: replace useEffect(closeAll/setOpen, [pathname])
with the state-during-render pattern (prevPathname comparison) — no extra
render pass, no lint warning.

ThemeToggle, FadeIn, TiltCard, ChatWindow, VisitorMonitor, StatusDashboard:
add targeted eslint-disable-next-line comments with explanations. These are
legitimate patterns that cannot be refactored away:
  - setMounted(true) / setReduceEffects: SSR hydration guards — window.matchMedia
    and next-themes are unavailable during the static build, so the effect is
    the only safe place to read the real client value.
  - setMessages / load() / runChecks(): post-mount localStorage restores and
    async data fetches whose setState calls are deferred, not synchronous cascades.

Hero.tsx: remove unused resumePdfUrl and resumeDocxUrl imports.
