fix: resolve all ESLint lint errors and add missing footer spacing

- ScrollProgress: use lazy useState initialiser (detectNativeScrollTimeline)
  instead of setting state inside a useEffect, and add supportsNative to
  the effect dependency array (resolves exhaustive-deps warning)
- AppShell: replace useState+useEffect with useSyncExternalStore so the
  scene preference subscription never mutates a ref during render
- SystemMetrics: hoist Date.now() to a module-level constant (MODULE_LOAD_TIME)
  to keep the render function pure (resolves react-hooks/purity warning)
- CommandPalette: replace useEffect resetting activeIndex on query change with
  the state-during-render pattern; remove two stale jsx-a11y eslint-disable
  comments that were no longer needed after m.div migration
- globals.css + gallery/github/chat/status pages: add .page-content utility
  (padding-bottom 80px) and apply it to every subpage container that was
  missing bottom clearance before the footer
